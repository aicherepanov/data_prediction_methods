»нструкции
«адание по вариантам, данные приведены в таблице, но дл€ студентов всегда муторно из таблицы данные забирать,
поэтому € еще прикладываю файл for_lab3.xlsx, где все эти данные по вариантам (уже по строкам!!!) переведены в таблицу.
„тобы загрузить данные дл€ своего варианта используйте такой код:

import pandas as pd
table = pd.read_excel('for_lab3.xlsx')
variant = 13 # номер варианта, например, 13
X = np.array(table.values[variant-1][1:])
print(X)