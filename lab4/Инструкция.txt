Инструкции
Добрый день, товарищи студенты. Выдаю новое задание - на прогноз регрессией. Все очень сильно похоже на предыдущую лабу. Хочу отметить два момента:
1) приложил данные для прогноза Fort.mat. Если будут проблемы с обработкой массива, при загрузке данных добавьте еще строчку:
file = h5py.File('Fort.mat', 'r')
data = file.get('Fort')
Fort = np.array(data)
Fort.ravel()
2) п. 14 - творческий, потому вызывает больше всего вопросов. Подсказка: для метода curve_fit надо определить периодическую функцию, например, вида:
a*np.sin(np.pi/6*t + b) + ...
и возможно даже вручную поиграться с параметрами внутри синуса.